# Shared secret (same as manager’s JWT_SECRET)
JWT_SECRET=supersecretkey

# Docker socket (usually the default)
DOCKER_SOCKET=unix:///var/run/docker.sock

MANAGER_URL=http://manager:8000        # Base URL of your Machine-Lab Manager
HOST_ID=                             # UUID of this host (fill after registration)
SERVER_KEY=                          # X-Server-Key token returned at host registration
HEARTBEAT_INTERVAL=10               # Seconds between heartbeats